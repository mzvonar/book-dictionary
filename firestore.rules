rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    function resourceMatchesUID() {
      return request.auth.uid == resource.data.userId;
    }

    match /{document=**} {
      allow read, write, update, delete: if resourceMatchesUID();
    }
  }
}